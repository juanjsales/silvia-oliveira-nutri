name: Quality gate

on:
  push:
    branches:
      - main
      - codex/product-platform
      - codex/evolucao-seguranca-arquitetura
  pull_request:

permissions:
  contents: read

concurrency:
  group: quality-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    name: Tests, types and production builds
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run check
      - name: Install browser for navigation tests
        run: npx playwright install --with-deps chromium
      - name: Validate critical navigation
        run: npm --workspace frontend-react run test:e2e:ci
      - name: Validate Vercel configuration
        run: node -e "JSON.parse(require('node:fs').readFileSync('vercel.json','utf8'))"
