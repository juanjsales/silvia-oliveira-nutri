import { AlertTriangle, CheckCircle2, RefreshCw, ShieldAlert } from 'lucide-react';
import { useCallback, useEffect, useState } from 'react';
import { api } from '../lib/api';

type Incident={id:string;requestId:string;method:string;route:string;errorName:string;errorCode?:string|null;occurredAt:string;resolvedAt?:string|null};

export function IncidentPanel(){const[items,setItems]=useState<Incident[]>([]);const[loading,setLoading]=useState(true);const[error,setError]=useState('');const load=useCallback(async()=>{setLoading(true);setError('');try{setItems((await api<{data:Incident[]}>('/api/monitoring/incidents')).data)}catch(c){setError(c instanceof Error?c.message:'Não foi possível consultar incidentes.')}finally{setLoading(false)}},[]);useEffect(()=>{void load()},[load]);async function resolve(id:string){await api(`/api/monitoring/incidents/${id}/resolve`,{method:'PATCH'});await load()}const open=items.filter(i=>!i.resolvedAt);return <section className="panel settings-section incident-panel"><header><ShieldAlert/><div><h2>Saúde operacional</h2><p>Falhas internas recentes, sem conteúdo clínico ou credenciais.</p></div><button type="button" className="icon-button" onClick={()=>void load()} disabled={loading} aria-label="Atualizar incidentes"><RefreshCw className={loading?'spin':''}/></button></header>{error&&<div className="form-error">{error}</div>}{!error&&<div className={`incident-summary ${open.length?'attention':'healthy'}`}>{open.length?<AlertTriangle/>:<CheckCircle2/>}<strong>{open.length?`${open.length} incidente(s) aguardando revisão`:'Nenhum incidente aberto'}</strong></div>}<div className="incident-list">{items.slice(0,10).map(item=><article key={item.id} className={item.resolvedAt?'resolved':''}><div><strong>{item.method} {item.route}</strong><span>{item.errorName}{item.errorCode?` · ${item.errorCode}`:''} · {new Date(item.occurredAt).toLocaleString('pt-BR')}</span><small>Referência: {item.requestId}</small></div>{!item.resolvedAt&&<button className="secondary-button" onClick={()=>void resolve(item.id)}>Marcar resolvido</button>}</article>)}</div></section>}
