import{CheckCircle2,Mail,Save,Send,ShieldCheck}from'lucide-react';import{useEffect,useState,type FormEvent}from'react';import{api}from'../lib/api';
type Smtp={host:string;port:number;secure:boolean;user:string;from:string;enabled:boolean;passwordConfigured:boolean};
const initial:Smtp={host:'',port:587,secure:false,user:'',from:'',enabled:false,passwordConfigured:false};
export function SmtpSettings(){const[form,setForm]=useState(initial);const[password,setPassword]=useState('');const[testEmail,setTestEmail]=useState('');const[busy,setBusy]=useState(false);const[message,setMessage]=useState('');const[error,setError]=useState('');useEffect(()=>{api<{data:Smtp}>('/api/settings/smtp').then(r=>{setForm({...initial,...r.data});setTestEmail(r.data.from||'')}).catch(c=>setError(c instanceof Error?c.message:'Erro ao carregar SMTP.'))},[]);async function save(e:FormEvent){e.preventDefault();setBusy(true);setError('');setMessage('');try{const r=await api<{message:string}>('/api/settings/smtp',{method:'PUT',body:JSON.stringify({...form,password:password||undefined})});setMessage(r.message);setPassword('');setForm(x=>({...x,passwordConfigured:x.passwordConfigured||!!password}))}catch(c){setError(c instanceof Error?c.message:'Não foi possível salvar o SMTP.')}finally{setBusy(false)}}async function test(){setBusy(true);setError('');setMessage('');try{const r=await api<{message:string}>('/api/settings/smtp/test',{method:'POST',body:JSON.stringify({to:testEmail})});setMessage(r.message)}catch(c){setError(c instanceof Error?c.message:'Falha no teste de envio.')}finally{setBusy(false)}}return <section className="panel settings-section smtp-settings"><header><Mail/><div><h2>E-mail e SMTP</h2><p>Configure os e-mails de acesso, consultas e recuperação de senha.</p></div></header>{error&&<div className="form-error">{error}</div>}{message&&<div className="form-success"><CheckCircle2/>{message}</div>}<form onSubmit={save}><div className="settings-grid"><label>Servidor SMTP<input value={form.host} onChange={e=>setForm({...form,host:e.target.value})} placeholder="smtp.seuprovedor.com" required/></label><label>Porta<input type="number" min="1" max="65535" value={form.port} onChange={e=>setForm({...form,port:Number(e.target.value)})} required/></label><label>Usuário<input value={form.user} onChange={e=>setForm({...form,user:e.target.value})} autoComplete="username" required/></label><label>Senha SMTP<input type="password" value={password} onChange={e=>setPassword(e.target.value)} autoComplete="new-password" placeholder={form.passwordConfigured?'Senha protegida — deixe vazio para manter':'Informe a senha SMTP'} required={!form.passwordConfigured}/></label><label>Remetente<input type="email" value={form.from} onChange={e=>setForm({...form,from:e.target.value})} placeholder="consultorio@dominio.com" required/></label><label>Segurança<select value={form.secure?'ssl':'starttls'} onChange={e=>setForm({...form,secure:e.target.value==='ssl'})}><option value="starttls">STARTTLS (porta 587)</option><option value="ssl">SSL/TLS (porta 465)</option></select></label><label className="smtp-toggle"><input type="checkbox" checked={form.enabled} onChange={e=>setForm({...form,enabled:e.target.checked})}/><span><strong>Ativar envio pelo sistema</strong><small>Use esta configuração nos e-mails automáticos.</small></span></label></div><div className="smtp-actions"><span><ShieldCheck/>A senha é cifrada e nunca é exibida novamente.</span><button className="primary-button" disabled={busy}><Save/> Salvar SMTP</button></div></form><div className="smtp-test"><label>E-mail para teste<input type="email" value={testEmail} onChange={e=>setTestEmail(e.target.value)} placeholder="destino@exemplo.com"/></label><button type="button" className="secondary-button" onClick={test} disabled={busy||!testEmail}><Send/> Enviar teste</button></div></section>}
