import{CheckCircle2,KeyRound}from'lucide-react';import{useState,type FormEvent}from'react';import{Link,useSearchParams}from'react-router-dom';import{api}from'../lib/api';import{AuthPage}from'./PasswordRecoveryPage';
export function PasswordResetPage(){const[params]=useSearchParams();const token=params.get('token')||'';const[password,setPassword]=useState('');const[confirmation,setConfirmation]=useState('');const[busy,setBusy]=useState(false);const[done,setDone]=useState(false);const[error,setError]=useState('');async function submit(e:FormEvent){e.preventDefault();if(password!==confirmation){setError('As senhas não coincidem.');return}setBusy(true);setError('');try{await api('/api/auth/password-reset',{method:'POST',body:JSON.stringify({token,password})});setDone(true)}catch(c){setError(c instanceof Error?c.message:'Link inválido ou expirado.')}finally{setBusy(false)}}return <AuthPage><form className="login-card" onSubmit={submit}><KeyRound className="auth-form-icon"/><span className="eyebrow">Nova credencial</span><h2>Defina sua nova senha</h2>{done?<><div className="form-success"><CheckCircle2/>Senha redefinida com sucesso.</div><Link className="primary-button login-submit" to="/login">Entrar no sistema</Link></>:<><p className="muted">Use no mínimo 12 caracteres e evite reutilizar senhas de outros serviços.</p>{!token&&<div className="form-error">O link não contém um token válido.</div>}<label>Nova senha<input type="password" minLength={12} maxLength={128} value={password} onChange={e=>setPassword(e.target.value)} required/></label><label>Confirmar nova senha<input type="password" minLength={12} value={confirmation} onChange={e=>setConfirmation(e.target.value)} required/></label>{error&&<div className="form-error">{error}</div>}<button className="primary-button login-submit" disabled={busy||!token}>{busy?'Salvando...':'Redefinir senha'}</button></>}</form></AuthPage>}
