import{ArrowLeft,Mail,ShieldCheck}from'lucide-react';import{useState,type FormEvent,type ReactNode}from'react';import{Link}from'react-router-dom';import{api}from'../lib/api';import{ClinicMark,useClinic}from'../contexts/ClinicContext';
export function PasswordRecoveryPage(){const[identifier,setIdentifier]=useState('');const[busy,setBusy]=useState(false);const[message,setMessage]=useState('');const[error,setError]=useState('');async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError('');try{const r=await api<{message:string}>('/api/auth/password-recovery',{method:'POST',body:JSON.stringify({identifier})});setMessage(r.message)}catch(c){setError(c instanceof Error?c.message:'Não foi possível solicitar a recuperação.')}finally{setBusy(false)}}return <AuthPage><form className="login-card" onSubmit={submit}><Mail className="auth-form-icon"/><span className="eyebrow">Recuperação de acesso</span><h2>Esqueceu sua senha?</h2><p className="muted">Informe o e-mail ou CPF usado no cadastro. Se a conta existir, enviaremos um link temporário.</p><label>E-mail ou CPF<input value={identifier} onChange={e=>setIdentifier(e.target.value)} required autoComplete="username"/></label>{message&&<div className="form-success"><ShieldCheck/>{message}</div>}{error&&<div className="form-error">{error}</div>}<button className="primary-button login-submit" disabled={busy}>{busy?'Enviando...':'Enviar instruções'}</button><Link className="auth-back" to="/login"><ArrowLeft/> Voltar ao login</Link></form></AuthPage>}
export function AuthPage({children}:{children:ReactNode}){const clinic=useClinic();return <main className="standalone-auth"><div className="auth-mini-brand"><ClinicMark className="auth-brand-mark"/><div><strong>{clinic.clinicName}</strong><small>{clinic.specialty}</small></div></div>{children}</main>}
