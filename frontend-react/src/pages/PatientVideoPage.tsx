import{ArrowLeft,ShieldCheck,Video}from'lucide-react';import{useEffect,useState}from'react';import{Link,useParams}from'react-router-dom';import{api}from'../lib/api';
export function PatientVideoPage(){const{id}=useParams();const[url,setUrl]=useState('');const[expires,setExpires]=useState('');const[error,setError]=useState('');useEffect(()=>{if(id)api<{data:{roomUrl:string;expiresAt:string}}>(`/api/video/appointments/${id}/access`,{method:'POST'}).then(r=>{setUrl(r.data.roomUrl);setExpires(r.data.expiresAt)}).catch(c=>setError(c instanceof Error?c.message:'Não foi possível entrar na sala.'))},[id]);return <main className="patient-video-page"><header><Link to="/portal"><ArrowLeft/> Voltar ao portal</Link><div><ShieldCheck/><span>Acesso validado pela sua sessão</span></div></header>{error?<section className="video-access-error"><Video/><h2>Sala indisponível</h2><p>{error}</p><Link className="primary-button" to="/portal">Voltar</Link></section>:url?<><iframe src={url} title="Videochamada nutricional" allow="camera; microphone; fullscreen; display-capture; autoplay"/><small>Acesso válido até {new Date(expires).toLocaleTimeString('pt-BR',{hour:'2-digit',minute:'2-digit'})}.</small></>:<div className="page-loader"><span className="spinner"/><p>Validando consulta e horário...</p></div>}</main>}
