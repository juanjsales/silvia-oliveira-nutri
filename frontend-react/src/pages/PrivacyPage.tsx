import { ArrowLeft, CheckCircle2, Download, FileWarning, ShieldCheck } from 'lucide-react';
import { useEffect, useState, type FormEvent } from 'react';
import { Link } from 'react-router-dom';
import { api } from '../lib/api';
type Notice={notice:{version:string;title:string;summary:string};acknowledged:boolean;acknowledgedAt?:string|null};
export function PrivacyPage(){const[data,setData]=useState<Notice|null>(null);const[error,setError]=useState('');const[notice,setNotice]=useState('');const[busy,setBusy]=useState(false);useEffect(()=>{api<{data:Notice}>('/api/privacy/notice').then(r=>setData(r.data)).catch(c=>setError(c instanceof Error?c.message:'Não foi possível abrir a área de privacidade.'))},[]);async function acknowledge(){setBusy(true);try{await api('/api/privacy/acknowledge',{method:'POST'});setData(x=>x?{...x,acknowledged:true,acknowledgedAt:new Date().toISOString()}:x);setNotice('Ciência registrada com sucesso.')}catch(c){setError(c instanceof Error?c.message:'Não foi possível registrar.')}finally{setBusy(false)}}async function download(){setBusy(true);try{const response=await fetch('/api/privacy/export',{credentials:'include'});if(!response.ok)throw new Error('Não foi possível preparar a exportação.');const blob=await response.blob(),url=URL.createObjectURL(blob),anchor=document.createElement('a');anchor.href=url;anchor.download=`meus-dados-${new Date().toISOString().slice(0,10)}.json`;anchor.click();URL.revokeObjectURL(url);setNotice('Arquivo preparado e baixado com segurança.')}catch(c){setError(c instanceof Error?c.message:'Falha na exportação.')}finally{setBusy(false)}}async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);const form=event.currentTarget,d=new FormData(form);try{await api('/api/privacy/requests',{method:'POST',body:JSON.stringify({type:String(d.get('type')),details:String(d.get('details')||'')})});form.reset();setNotice('Solicitação enviada para análise do consultório.')}catch(c){setError(c instanceof Error?c.message:'Não foi possível enviar a solicitação.')}finally{setBusy(false)}}return <main className="patient-portal privacy-page"><header className="privacy-header"><Link className="ghost-button" to="/portal"><ArrowLeft/> Voltar ao portal</Link><span><ShieldCheck/> Privacidade e seus dados</span></header>{error&&<div className="form-error">{error}</div>}{notice&&<div className="form-success"><CheckCircle2/>{notice}</div>}{data&&<div className="privacy-layout"><section className="panel privacy-notice"><span>Versão {data.notice.version}</span><h1>{data.notice.title}</h1><p>{data.notice.summary}</p>{data.acknowledged?<div className="privacy-ack"><CheckCircle2/> Ciência registrada {data.acknowledgedAt?`em ${new Date(data.acknowledgedAt).toLocaleDateString('pt-BR')}`:''}</div>:<button className="primary-button" onClick={()=>void acknowledge()} disabled={busy}><ShieldCheck/> Li e estou ciente</button>}</section><section className="panel privacy-actions"><h2>Acessar meus dados</h2><p>Baixe uma cópia estruturada dos dados mantidos no portal. Arquivos de exames permanecem protegidos e não são incluídos no download.</p><button className="secondary-button" onClick={()=>void download()} disabled={busy}><Download/> Baixar meus dados</button></section><form className="panel privacy-request" onSubmit={submit}><div className="privacy-request-intro"><FileWarning/><div><h2>Solicitar análise</h2><p>Pedidos de exclusão são avaliados antes da execução devido às obrigações de conservação do prontuário.</p></div></div><label>Tipo<select name="type"><option value="ACCESS">Acesso</option><option value="CORRECTION">Correção</option><option value="DELETION">Análise de exclusão</option></select></label><label>Detalhes<textarea name="details" maxLength={2000} required/></label><button className="primary-button" disabled={busy}>Enviar solicitação</button></form></div>}</main>}
