import { BadgeCheck, FileCheck2, Pill, Printer, Stethoscope } from 'lucide-react';
import { useEffect, useMemo, useState, type CSSProperties, type ReactNode } from 'react';
import { api } from '../lib/api';
type Patient={id:string;name:string;cpf?:string|null};type EncounterList={id:string;patientId:string;patientName:string;status:string;startedAt:string};type Supplement={id:string;name:string;dosage?:string;posology?:string;pharmaceuticalForm?:string;observation?:string};type Encounter={id:string;patientName:string;supplements:Supplement[]};type Settings={clinicName:string;professionalName:string;crn:string;specialty:string;phone?:string;email?:string;address?:string;city?:string;logoUrl?:string;primaryColor:string;secondaryColor:string;documentFooter:string};type Kind='declaration'|'certificate'|'supplements';
const today=new Date().toISOString().slice(0,10);const fmt=(date:string)=>new Date(`${date}T12:00:00`).toLocaleDateString('pt-BR',{day:'2-digit',month:'long',year:'numeric'});
export function DocumentsPage(){const[patients,setPatients]=useState<Patient[]>([]);const[encounters,setEncounters]=useState<EncounterList[]>([]);const[settings,setSettings]=useState<Settings|null>(null);const[kind,setKind]=useState<Kind>('declaration');const[patientId,setPatientId]=useState('');const[encounterId,setEncounterId]=useState('');const[encounter,setEncounter]=useState<Encounter|null>(null);const[date,setDate]=useState(today);const[start,setStart]=useState('14:00');const[end,setEnd]=useState('15:00');const[observation,setObservation]=useState('');const[error,setError]=useState('');useEffect(()=>{Promise.all([api<{data:Patient[]}>('/api/patients'),api<{data:EncounterList[]}>('/api/encounters'),api<{data:Settings}>('/api/settings')]).then(([p,e,s])=>{setPatients(p.data);setEncounters(e.data);setSettings(s.data)}).catch(c=>setError(c instanceof Error?c.message:'Erro ao carregar documentos.'))},[]);useEffect(()=>{if(encounterId)api<{data:Encounter}>(`/api/encounters/${encounterId}`).then(r=>setEncounter(r.data)).catch(c=>setError(c instanceof Error?c.message:'Erro ao abrir prescrição.'));else setEncounter(null)},[encounterId]);const patient=useMemo(()=>patients.find(p=>p.id===patientId),[patients,patientId]);const patientEncounters=encounters.filter(e=>!patientId||e.patientId===patientId);const style=settings?{"--doc-primary":settings.primaryColor,"--doc-secondary":settings.secondaryColor} as CSSProperties:undefined;function print(){window.print()}return <div className="documents-center"><div className="page-intro"><p>Emita documentos oficiais com identidade e dados profissionais padronizados.</p><button className="primary-button" onClick={print} disabled={!settings||(!patient&&kind!=='supplements')||(kind==='supplements'&&!encounter)}><Printer size={17}/> Imprimir / Salvar PDF</button></div>{error&&<div className="form-error">{error}</div>}<div className="documents-layout"><section className="panel document-controls"><div className="document-kind-tabs"><button className={kind==='declaration'?'active':''} onClick={()=>setKind('declaration')}><FileCheck2/> Declaração</button><button className={kind==='certificate'?'active':''} onClick={()=>setKind('certificate')}><Stethoscope/> Atestado</button><button className={kind==='supplements'?'active':''} onClick={()=>setKind('supplements')}><Pill/> Suplementação</button></div><label>Paciente<select value={patientId} onChange={e=>{setPatientId(e.target.value);setEncounterId('')}}><option value="">Selecione o paciente</option>{patients.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select></label>{kind==='supplements'?<label>Atendimento<select value={encounterId} onChange={e=>setEncounterId(e.target.value)}><option value="">Selecione o atendimento</option>{patientEncounters.map(e=><option key={e.id} value={e.id}>{new Date(e.startedAt).toLocaleDateString('pt-BR')} · {e.patientName}</option>)}</select></label>:<><div className="control-row"><label>Data<input type="date" value={date} onChange={e=>setDate(e.target.value)}/></label><label>Início<input type="time" value={start} onChange={e=>setStart(e.target.value)}/></label><label>Fim<input type="time" value={end} onChange={e=>setEnd(e.target.value)}/></label></div><label>Observações<textarea rows={4} value={observation} onChange={e=>setObservation(e.target.value)} placeholder="Recomendações ou informações adicionais"/></label></>}</section><section className="document-preview-shell" style={style}>{settings&&(kind==='supplements'?encounter?<SupplementDocument settings={settings} encounter={encounter}/>:<PreviewEmpty text="Selecione um atendimento com suplementação."/>:patient?<OfficialDocument settings={settings} patient={patient} kind={kind} date={date} start={start} end={end} observation={observation}/>:<PreviewEmpty text="Selecione um paciente para visualizar o documento."/>)}</section></div></div>}
function Sheet({settings,children,label}:{settings:Settings;children:ReactNode;label:string}){return <article className="official-sheet"><header><div>{settings.logoUrl?<img src={settings.logoUrl} alt=""/>:<span>N</span>}<div><strong>{settings.professionalName}</strong><small>{settings.specialty} · {settings.crn}</small></div></div><em>{label}</em></header><main>{children}</main><footer><div><strong>{settings.documentFooter}</strong><span>{[settings.phone,settings.email,settings.address].filter(Boolean).join(' · ')}</span></div></footer></article>}
function OfficialDocument({settings,patient,kind,date,start,end,observation}:{settings:Settings;patient:Patient;kind:Exclude<Kind,'supplements'>;date:string;start:string;end:string;observation:string}){const declaration=kind==='declaration';return <Sheet settings={settings} label={declaration?'Declaração':'Atestado nutricional'}><section className="official-content"><BadgeCheck/><span>{declaration?'Documento de comparecimento':'Documento de acompanhamento'}</span><h1>{declaration?'Declaração de comparecimento':'Atestado nutricional'}</h1><div className="official-text">{declaration?<p>Declaro, para os devidos fins, que <strong>{patient.name}</strong>{patient.cpf?`, inscrito(a) no CPF sob o nº ${patient.cpf},`:','} compareceu à consulta nutricional no dia <strong>{fmt(date)}</strong>, no período das <strong>{start}</strong> às <strong>{end}</strong>.</p>:<><p>Atesto, para os devidos fins, que <strong>{patient.name}</strong>{patient.cpf?`, inscrito(a) no CPF sob o nº ${patient.cpf},`:','} encontra-se sob acompanhamento nutricional clínico nesta data, <strong>{fmt(date)}</strong>.</p><p>Recomenda-se a observância do plano alimentar e das orientações nutricionais individualizadas fornecidas durante o acompanhamento.</p></>}{observation&&<aside><strong>Observações</strong><p>{observation}</p></aside>}</div><div className="signature"><span>{settings.city||'Rio de Janeiro'}, {fmt(date)}</span><i/><strong>{settings.professionalName}</strong><small>Nutricionista · {settings.crn}</small></div></section></Sheet>}
function SupplementDocument({settings,encounter}:{settings:Settings;encounter:Encounter}){return <Sheet settings={settings} label="Prescrição de suplementação"><section className="supplement-doc"><span>Prescrição individualizada</span><h1>{encounter.patientName}</h1>{encounter.supplements.length?<div>{encounter.supplements.map((s,index)=><article key={s.id}><b>{String(index+1).padStart(2,'0')}</b><div><h2>{s.name}</h2><strong>{s.dosage||'Dosagem conforme orientação'}</strong><p>{s.posology||'Posologia não informada'}</p>{s.pharmaceuticalForm&&<small>Forma: {s.pharmaceuticalForm}</small>}{s.observation&&<aside>{s.observation}</aside>}</div></article>)}</div>:<p className="no-prescription">Nenhuma suplementação registrada neste atendimento.</p>}<div className="signature"><i/><strong>{settings.professionalName}</strong><small>Nutricionista · {settings.crn}</small></div></section></Sheet>}
function PreviewEmpty({text}:{text:string}){return <div className="document-preview-empty"><FileCheck2/><strong>Prévia do documento</strong><p>{text}</p></div>}
