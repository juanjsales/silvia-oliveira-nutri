import{test,expect}from'@playwright/test';const databaseUrl=process.env.E2E_DATABASE_URL||'';const safe=/test|testing|homolog/i.test(databaseUrl);test.describe('fluxo clínico com banco isolado',()=>{test.skip(!safe,'Defina E2E_DATABASE_URL apontando explicitamente para um banco de teste ou homologação.');test('database safety guard is active',async()=>{expect(safe).toBe(true);expect(databaseUrl).not.toEqual(process.env.DATABASE_URL)})});
