export function validateFirstStaging(input){const failures=[];if(input.branch!=='codex/product-platform')failures.push('STAGING_BRANCH_INVALID');if(input.deploymentEnvironment!=='staging')failures.push('STAGING_ENVIRONMENT_INVALID');if(!input.protectedProjectId||!input.protectedDatabaseId)failures.push('PRODUCTION_FINGERPRINTS_MISSING');if(!input.previewProjectId||input.previewProjectId===input.protectedProjectId)failures.push('PREVIEW_PROJECT_NOT_ISOLATED');if(!input.previewDatabaseId||input.previewDatabaseId===input.protectedDatabaseId)failures.push('PREVIEW_DATABASE_NOT_ISOLATED');if(!input.previewUrl||!/^https:\/\/[^/]+\.vercel\.app\/?$/i.test(input.previewUrl)||input.forbiddenIdentifiers?.some(x=>input.previewUrl.toLowerCase().includes(x.toLowerCase())))failures.push('PREVIEW_URL_INVALID');for(const gate of['artifactVerified','environmentResolved','fakeSmokePassed','promotionDisabled','migrationExecutionDisabled'])if(input[gate]!==true)failures.push(`GATE_REQUIRED:${gate}`);if(input.environmentTargets?.some(target=>target!=='preview'))failures.push('ENV_TARGET_NOT_PREVIEW');return failures}
export function assertFirstStagingReady(input){const failures=validateFirstStaging(input);if(failures.length)throw Object.assign(new Error(`Primeiro staging bloqueado: ${failures.join(', ')}`),{failures});return{ready:true,checkedAt:new Date().toISOString()}}
