import assert from'node:assert/strict';import test from'node:test';import{assertFirstStagingReady,validateFirstStaging}from'./first-staging-checklist.mjs';const valid={branch:'codex/product-platform',deploymentEnvironment:'staging',protectedProjectId:'prod-project',protectedDatabaseId:'prod-db',previewProjectId:'demo-project',previewDatabaseId:'demo-db',previewUrl:'https://demo-staging.vercel.app',forbiddenIdentifiers:['silviaoliveira.vercel.app'],artifactVerified:true,environmentResolved:true,fakeSmokePassed:true,promotionDisabled:true,migrationExecutionDisabled:true,environmentTargets:['preview']};
test('checklist aprova somente primeiro staging inteiramente isolado',()=>assert.equal(assertFirstStagingReady(valid).ready,true));
test('checklist bloqueia branch, projeto, banco, domínio ou target de produção',()=>{for(const mutation of[{branch:'main'},{previewProjectId:'prod-project'},{previewDatabaseId:'prod-db'},{previewUrl:'https://silviaoliveira.vercel.app'},{environmentTargets:['preview','production']}])assert.ok(validateFirstStaging({...valid,...mutation}).length>0)});
test('todos os gates técnicos são obrigatórios e fail-closed',()=>{for(const gate of['artifactVerified','environmentResolved','fakeSmokePassed','promotionDisabled','migrationExecutionDisabled'])assert.throws(()=>assertFirstStagingReady({...valid,[gate]:false}),new RegExp(gate))});
